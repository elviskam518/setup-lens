[project]
name = "atlas-api-demo"
version = "0.0.0"
requires-python = ">=3.11"

[project.scripts]
atlas-api = "atlas_api.main:serve"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
